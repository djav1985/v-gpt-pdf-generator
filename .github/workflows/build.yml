name: Build and Push API Image

on:
  push:
    branches:
      - 'main'
      - 'dev'
  release:
    types: [published]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3 # Latest as of now

      - name: Login to Docker Hub
        uses: docker/login-action@v2 # Updated to v2
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3 # Updated to v3

      - name: Extract metadata (tags, labels) for Docker
        uses: docker/metadata-action@v3 # Latest as of now
        with:
          images: vontainment/gpt-pdf-generator
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,priority=100
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.ref_name }},enable=${{ github.event_name == 'push' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v3 # Updated to v3
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=vontainment/gpt-pdf-generator:${{ github.ref_name }}
          cache-to: type=inline,mode=max
